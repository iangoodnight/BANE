<nav class="navbar navbar-expand-lg fixed-top navbar-dark bg-dark flex-md-nowrap shadow">
{{!-- 	<div class="container-fluid"> --}}
	<a class="navbar-brand col-md-2" style="padding-left: 0px;" href="#">BANE</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#header" 	aria-controls="header" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>
	
	<div class="collapse navbar-collapse" id="header">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item {{#if active_home}}active{{/if}}">
				<a class="nav-link" href="/">
					Home 
					{{#if active_home}}
						<span class="sr-only">(current)</span>
					{{/if}}
				</a>
			</li>
			<li class="nav-item {{#if active_directory}}active{{/if}}">
				<a class="nav-link" href="/directory">
					Directory 
					{{#if active_directory}}
						<span class="sr-only">(current)</span>
					{{/if}}
				</a>
			</li>
			{{#if user}}
			<li class="nav-item {{#if active_dashboard}}active{{/if}}">
				<a class="nav-link" href="/dashboard">
					Dashboard 
					{{#if active_dashboard}}
						<span class="sr-only">(current)</span>
					{{/if}}
				</a>
			</li>
			{{/if}}
			{{#if admin}}
			<li class="nav-item {{#if active_admin}}active{{/if}}">
				<a class="nav-link" href="/admin">
					Admin 
					{{#if active_admin}}
						<span class="sr-only">(current)</span>
					{{/if}}
				</a>
			</li>
			{{/if}}
		</ul>
		{{#if user}}
		<a class="nav-link mr-sm-2" id="logout" href="/">
			Logout
		</a>
		{{else}}
		<a class="nav-link mr-sm-2" href="/login">
			Login
		</a>
		{{/if}}
	</div>
{{!-- 	</div> --}}
</nav>
